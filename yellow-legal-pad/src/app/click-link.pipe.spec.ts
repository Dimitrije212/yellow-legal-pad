import { ClickLinkPipe } from './click-link.pipe';

describe('ClickLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ClickLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
