import { NoteFocusDirective } from './note-focus.directive';

describe('NoteFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteFocusDirective();
    expect(directive).toBeTruthy();
  });
});
